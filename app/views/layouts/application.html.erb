<!DOCTYPE html>
<html>
  <head>
    <title>Lani - Project Management Platform</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-50">
    <%= render 'shared/navigation' %>
    
    <% if flash.any? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-4">
        <% flash.each do |type, message| %>
          <div class="mb-4 p-4 rounded-md <%= type == 'notice' ? 'bg-green-50 border border-green-200' : 'bg-red-50 border border-red-200' %>">
            <div class="flex">
              <div class="flex-shrink-0">
                <% if type == 'notice' %>
                  <svg class="h-5 w-5 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
                  </svg>
                <% else %>
                  <svg class="h-5 w-5 text-red-400" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
                  </svg>
                <% end %>
              </div>
              <div class="ml-3">
                <p class="text-sm font-medium <%= type == 'notice' ? 'text-green-800' : 'text-red-800' %>">
                  <%= message %>
                </p>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    <% end %>
    
    <main class="min-h-screen">
      <%= yield %>
    </main>
  </body>
</html>
